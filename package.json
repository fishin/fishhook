{
  "name": "fishhook",
  "version": "0.0.3",
  "description": "scheduler utilities for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/fishin/fishhook"
  },
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "test": "lab -a code -t 100 -v -m 10000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log",
    "json": "lab -a code -t 100 -v -m 10000 -L"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "dependencies": {
    "cron": "1.0.x",
    "hoek": "2.x.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
